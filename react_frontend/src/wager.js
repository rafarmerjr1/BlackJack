/*
import './App.css';
import React, { useEffect, useState } from 'react'
import Game, { getNewGame } from './game'
import { createRoot } from 'react-dom/client';

export default function Wager(props) {
  newGame();
return (
    <div className="App">
        <header className="App-header">
        <h1>Place Your Bet</h1>
        <p> I will bet the same amount. </p>
        <p>Your balance is ${props.props.balance}.</p>
        <p>{props.props}</p>
        <form onSubmit={handleSubmit}>
        <input
             type="text"
             name="bet"
             value={this.state.wager}
             onChange={this.handleChange}
             />
        <input type="submit" value="Place Bet"/>
        </form>
          </header>
      </div>
);
}





  <div className="App">
    <header className="App-header">
      <h1>Place Your Bet</h1>
      <p> I will bet the same amount. </p>
      <p>Your balance is ${props.data.balance}.</p>
      </header>
  </div>
);
}


      

  <div className="App">
    <header className="App-header">
      <h1>Place Your Bet</h1>
      <p> I will bet the same amount. </p>
      <p>Your balance is ${props.props.balance}.</p>
      <form onSubmit={handleSubmit}>
      <input
           type="text"
           name="bet"
           value={this.state.wager}
           onChange={this.handleChange)}
           />
      <input type="submit" value="Place Bet"/>
      </form>
  
      <h3>Dealer Hand</h3>
        <p>Dealer Score: {props.props.dealer_score}</p>
        <p>Dealer Card: This {props.props.gameState.dealer_card}</p>
        
        <h3>Player Hand</h3>
        <img src={require("./images/PNG-cards-1.3/10_of_clubs.png")} />
        <p>Player Score: {props.player_score}</p>
        <p>Player Card: Your {props.player_card}</p>
      </header>
  </div>
      */

